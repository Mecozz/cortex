# tasks

Task extraction and management module.

**TASKEXT** — extracts actionable tasks and todos from conversation turns via Claude Haiku.  
**TASKS** — stores, retrieves, and closes tasks scoped to a project.

Open tasks are injected into every system prompt by `inject::assemble()`.
